package com.spin.mobile

import org.scalatest.FunSpec

/**
 * To work on unit tests, switch the Test Artifact in the Build Variants view.
 */
class AndroidUnitTest extends FunSpec {

    describe("Example") {
        it("should be equal 4 = 2 + 2") {
            assert(4 == 2 + 2)
        }
    }
}